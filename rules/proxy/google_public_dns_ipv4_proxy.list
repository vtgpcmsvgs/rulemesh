# Google Public DNS 主 IPv4 端点节点选择规则。
# 维护原则：
# 1) 当前只精确匹配 8.8.8.8/32，不再绑定 53 端口，方便后续复用到其他端口探活。
# 2) 这不是 Google TW 区域规则的补充，而是精确 IP 的代理入口，避免与台湾策略绑定。
# 3) 客户端接入时必须放在 proxy/gfw 前，避免被广谱代理规则提前命中。
IP-CIDR,8.8.8.8/32
