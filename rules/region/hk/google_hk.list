# Google 通用服务兼容入口：按“完整上游域名 + 产品专项 + 激进关键词 + 官方 IP 地址空间”维护。
# 维护约束：
# 1) Google、YouTube、Google Play、Workspace、FCM 等服务使用全地区自动选择；路径保留以兼容已有订阅。
# 2) 本文件位于海外 AI 美国入口和国内精选直连之后、广告拒绝和广谱兜底之前。
# 3) Google AI 专项维护在 region/us/ai_us.list，必须先匹配美国入口。
# 4) 官方 goog.json 地址空间完整纳入，不扣除 Google Cloud 客户地址；第三方 GCP 服务也使用自动选择。
# 5) 普通 Google 与 Google DNS 连接均以性能优先；AI DNS 独立使用美国出口。

# Google 通用服务：完整上游主体优先，覆盖搜索、账号、Gmail、Workspace、静态资源与公共接口。
INCLUDE,upstream/blackmatrix7/google.list
DOMAIN-SUFFIX,google.com
DOMAIN-KEYWORD,google
DOMAIN-KEYWORD,googleapis
DOMAIN-KEYWORD,gstatic

# Google FCM：承接推送及移动设备相关入口。
INCLUDE,upstream/blackmatrix7/google_fcm.list

# Google Play 与 Android：显式兜住应用、资源包和下载重定向。
DOMAIN-SUFFIX,googleplay.com
DOMAIN-SUFFIX,googleusercontent.com
DOMAIN-SUFFIX,android.com
DOMAIN-SUFFIX,gvt3.com
DOMAIN-SUFFIX,xn--ngstr-lra8j.com

# YouTube：完整上游主体加品牌与媒体分发关键词兜底。
INCLUDE,upstream/blackmatrix7/youtube.list
DOMAIN-KEYWORD,youtube
DOMAIN-KEYWORD,googlevideo

# Google 官方公网地址空间：故意覆盖全部 IPv4 / IPv6，并保留 no-resolve 防止额外 DNS 查询。
INCLUDE,upstream/google/goog_ip_ranges.list
