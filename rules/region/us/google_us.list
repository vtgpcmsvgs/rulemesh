# Google 通用服务美国分流规则（主要入口）：按“同类服务聚合展示 + 上游优先 + 本地关键词兜底”维护。
# 维护约束：
# 1) Google 通用服务（含 Google Play / YouTube / FCM）主维护在本文件，面向美国 AI App 下载与账号地区一致性场景，客户端必须绑定美国节点。
# 2) 每个平台尽量把“上游 INCLUDE、通用关键词、产品边界说明”放在同一小节，便于快速定位与补规则。
# 3) Gemini / AI Studio / NotebookLM 允许在 rules/region/us/ai_us.list 保留 AI 视角交叉兜底，但客户端顺序仍必须让本文件先于 ai_us.list。
# 4) 本规则集设计目标固定为美国节点，客户端请绑定 US-AUTO（或等价美国策略组），不要再绑定台湾策略组。
# 5) 禁止再在 rules/proxy/google.list、旧台湾入口或其他文件重复维护 Google 通用规则。

# Google FCM：以上游主体为主，承接推送与相关移动入口。
INCLUDE,upstream/blackmatrix7/google_fcm.list

# Google Play 下载与资源分发：显式兜住 APK、资源包与下载重定向，避免落到后面的通用 direct/gfw。
DOMAIN-SUFFIX,googleplay.com
DOMAIN-SUFFIX,googleusercontent.com
DOMAIN-SUFFIX,android.com
DOMAIN-SUFFIX,gvt3.com
DOMAIN-SUFFIX,xn--ngstr-lra8j.com

# YouTube：以上游主体为主，辅以媒体分发关键词兜底。
INCLUDE,upstream/blackmatrix7/youtube.list
DOMAIN-KEYWORD,youtube
DOMAIN-KEYWORD,googlevideo

# Google 通用服务：保留品牌、公共接口与静态资源关键词兜底。
DOMAIN-SUFFIX,google.com
DOMAIN-KEYWORD,google
DOMAIN-KEYWORD,googleapis
DOMAIN-KEYWORD,gstatic

# Gemini / Google AI：保留 AI 品牌关键词；更细的 AI 产品交叉兜底继续放在 ai_us.list。
DOMAIN-KEYWORD,gemini
